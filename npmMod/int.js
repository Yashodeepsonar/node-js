// const validator = require('validator');
// const colors = require('colors');

// const res= validator.isEmail('foo@barm.com');
// console.log(res ? colors.green.inverse(res) : colors.red.inverse(res));
// console.log(colors.rainbow('Yash sonar')); 
